name: buildAndDeploy

on:
  # push:
  #   branches: [main]
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: echo 'Hello world !!'
      - name: step 2
        run: echo 'Something else'