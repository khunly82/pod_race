1. Create a dockerfile that will contain how to run code fastapi application

2. create a worflowfile that contains 1 job
    - build and ulpload image
        - checkout the code
        - build an image of the code
        - upload this image to a container registry (like dockerhub, gitlab, ...)